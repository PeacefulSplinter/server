var mongoose = require('mongoose');
var User = require('./userModel.js');

var integrationSchema = new mongoose.Schema({

	name: String,
	id: String,
	data: 

});

var Integration = mongoose.model('Integration', grantSchema);
